// Your Supabase project details — replace BOTH placeholder values below.
// Find them at: supabase.com → your project → Settings → API
//
//   SUPABASE_URL       = your "Project URL"
//   SUPABASE_ANON_KEY  = your "anon public" (publishable) key
//
// These two values are designed to be public — they ship in this file, which
// anyone can read on your GitHub Pages site. That is fine: see the README for
// what they can and cannot do.
//
// NEVER paste your `service_role` (secret) key here. That key bypasses all
// security rules. If it ever lands in public code, rotate it immediately in
// Supabase → Settings → API.

const SUPABASE_URL = 'https://YOUR-PROJECT-REF.supabase.co'
const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY'
